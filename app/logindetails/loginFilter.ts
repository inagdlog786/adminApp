// import {PipeTransform,Pipe} from '@angular/core';
// import { LogindetailsComponent } from './logindetails.component';

// @Pipe({
//     name:'LoginFilter'
// })
// export class LoginFilterPipe implements PipeTransform {
//     transform(value:Ilist[],args:string[]):Ilist[]{
//         let filter:string=args[0] ? args[0].toLocaleLowerCase():null;
//         return filter? value.filter((list:Ilist)=>
//         list.UserName.toLocaleLowerCase().indexOf(filter) !=1):value;
//     }

// }
